pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Checkout Completed'
            }
        }

        stage('Verify Project') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }

    }
}
